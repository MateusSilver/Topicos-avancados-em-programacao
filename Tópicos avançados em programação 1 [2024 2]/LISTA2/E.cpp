#include <bits/stdc++.h>
using namespace std;

#define loop(i,a,b) for(int i=a; i<b; i++)

int main(){
    int n, t;

    cin >> n;
    cin >> t;
    vector<int> v;

    int entrada;

    loop(i,0,n){
        cin >> entrada;
        v.push_back(entrada);
    }

    int p = 0;
    int soma = 0;

    while(soma < t){

    }
    cout << p << "\n";
}
