#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define loop(i,a,b) for(int i = a; i<b; i++)

const int MAXN = 2e5 + 100;
ll A[MAXN];
ll B[MAXN];

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    ll n;
    ll m;
    cin >> n >> m;
    loop(i,0,n){
        cin >> A[i];
    }
    loop(i,0,m){
        cin >> B[i];
    }

}

